// PostCSS es el motor que procesa el CSS. Aquí le decimos que use
// Tailwind (las clases de utilidad) y Autoprefixer (compatibilidad navegadores).
export default {
  plugins: {
    tailwindcss: {},
    autoprefixer: {},
  },
};
